# OnClick-BGColor-Changer
Clicking over the screen changes background color. 

Added event listener to the document object so as on every click a random rgb color is generated and applied to the background.
